# Strict Pacifist PC — exercises RP Constraints (Hard Filter / Tier 1).
#
# A pacifist PC with `pacifist_strict` is faced with an attacking goblin.
# The pacifist has ONLY weapon attacks in their action list, so the hard
# filter empties the candidate set every turn. Per §6.4 guaranteed-legal
# fallback, the runner logs `passed_turn` events instead of executing
# an action. The pacifist takes damage from the goblin but never strikes
# back — outcome is enemy victory or a stalemate with the pacifist down.
#
# Expected event log: each pacifist turn produces a `passed_turn` event
# with reason 'rp_hard_filter_empty_set' and zero attack_roll events
# from the pacifist.

id: pacifist_encounter

environment:
  template: open_battlefield

actors:
  - instance_id: goblin_attacker
    name: Goblin Attacker
    side: enemy
    template_ref:
      entity_type: monster
      id: m_goblin_warrior

  - instance_id: pacifist_monk
    name: Pacifist Monk
    side: pc
    template:
      id: pc_pacifist_monk
      name: Pacifist Monk
      source: user_authored
      combat:
        armor_class: 17
        hit_points:
          average: 30
          dice: "5d8"
          con_contribution: 5
        speed:
          walk: 40
        initiative:
          modifier: 3
          score: 15
      abilities:
        str: { score: 12, save: 1 }
        dex: { score: 16, save: 3 }
        con: { score: 12, save: 1 }
        int: { score: 10, save: 0 }
        wis: { score: 14, save: 5 }    # high WIS — won't auto-retreat
        cha: { score: 10, save: 0 }
      cr: { value: 1.0, xp: 200, proficiency_bonus: 2 }
      behavior_profile:
        rp_constraints:
          - id: pacifist_strict
      actions:
        # Only attack actions in the action list — pacifist_strict will
        # filter them ALL, leaving an empty candidate set every turn.
        - id: a_unarmed
          name: Unarmed Strike
          type: weapon_attack
          pipeline:
            - primitive: attack_roll
              params: { kind: melee, bonus: 5, reach_ft: 5 }
            - primitive: damage
              params: { dice: "1d4", modifier: 3, type: bludgeoning }
              when: { event: damage_roll, condition: "combat.attack_state == hit" }
